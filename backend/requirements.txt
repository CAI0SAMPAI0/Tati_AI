# FastAPI core
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
pydantic-settings==2.4.0

# AI
anthropic==0.40.0
groq==0.9.0
httpx==0.27.0          # async HTTP (ElevenLabs)

# Database
supabase==2.0.0

# Audio (fallback TTS)
gtts==2.5.3

# Security
bcrypt==4.1.2
python-dotenv==1.0.0

# File processing
pdfplumber==0.11.0
python-docx==1.1.0
Pillow==10.0.0

# RAG (futuramente)
# openai==1.x              # embeddings via API
# pgvector==0.3.x          # já no Supabase com extensão vector

# Dev
pytest==8.0.0
pytest-asyncio==0.23.0
httpx==0.27.0              # para testar endpoints
# Vercel ignora o Dockerfile do frontend — ela tem próprio sistema de build.
# Este arquivo serve apenas para indicar o diretório raiz do frontend.
# Configurações do deploy são feitas no painel da Vercel ou via vercel.json.
